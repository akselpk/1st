class Computing:
    '''Class for make a compute'''
    
    def adds_numbers(number:int=1) -> int:
        '''Takes a number (default value is 1), and adds 2'''
        return number + 2


print(adds_numbers(400))
